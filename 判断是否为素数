#include <stdio.h>

int main(void)
{
	int num = 0;
	int flag = 0;
	scanf("%d", &num);
	flag = num;
	
	for (int i = 2; i < num; i++)
	{
		if (num % i == 0)
			flag = 0;
	}
	if (flag == 0)
		printf("N!");
	else
		printf("Y!");

	return 0;
}
